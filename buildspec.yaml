version: 0.2

phases:
  install:
    commands:
      - echo "Step 1: Installing packages..."
      - apt-get update
      
  pre_build:
    commands:
      - echo "Step 2: Getting ready..."
      - echo "Current folder is:"
      - pwd
      
  build:
    commands:
      - echo "Step 3: Running my bash script..."
      - chmod +x ./script.sh
      - ./script.sh
      
  post_build:
    commands:
      - echo "Step 4: All done!"
